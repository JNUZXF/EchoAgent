# ========== Agent框架配置示例文件 ==========
# 复制此文件为.env并根据实际需求修改配置值
# 注意：请勿将包含敏感信息的.env文件提交到版本控制系统

# ========== 基础配置 ==========
# 用户唯一标识符（必填）
AGENT_USER_ID=your_user_id

# 智能体名称（可选，默认：echo_agent）
AGENT_NAME=echo_agent

# 对话会话ID（可选，每次对话可以不同）
# CONVERSATION_ID=session_20240101_001

# 工作空间名称（可选，默认使用files目录）
# AGENT_WORKSPACE=my_workspace

# 用户自定义系统提示词（可选）
# USER_SYSTEM_PROMPT="你是一个专业的AI助手，请遵循以下原则..."

# ========== 模型配置 ==========
# 主要对话模型（默认：doubao-seed-1-6-250615）
MAIN_MODEL=doubao-seed-1-6-250615

# 工具判断模型（默认：doubao-pro）
TOOL_MODEL=doubao-pro

# 快速响应模型（默认：doubao-pro）
FLASH_MODEL=doubao-pro

# ========== 性能配置 ==========
# 最大对话历史条数（默认：100）
MAX_HISTORY=100

# 每次对话最大令牌数（默认：8000）
MAX_TOKENS=8000

# 工具执行超时时间，单位秒（默认：30.0）
TOOL_TIMEOUT=30.0

# ========== 重试配置 ==========
# 最大重试次数（默认：3）
MAX_RETRIES=3

# 重试退避因子（默认：2.0）
RETRY_BACKOFF=2.0

# ========== 日志配置 ==========
# 日志级别：DEBUG, INFO, WARNING, ERROR, CRITICAL（默认：INFO）
LOG_LEVEL=INFO

# 是否启用文件日志（默认：true）
ENABLE_FILE_LOGGING=true

# 日志文件轮转大小（默认：10MB）
LOG_ROTATION_SIZE=10MB

# ========== 开发环境示例配置 ==========
# 开发环境可以使用更详细的日志和较小的限制进行调试

# 开发环境用户
# AGENT_USER_ID=dev_user

# 开发环境模型（可以使用更快的模型进行测试）
# MAIN_MODEL=doubao-pro
# TOOL_MODEL=doubao-pro  
# FLASH_MODEL=doubao-pro

# 开发环境性能配置（较小的值便于测试）
# MAX_HISTORY=50
# MAX_TOKENS=4000
# TOOL_TIMEOUT=15.0

# 开发环境日志配置
# LOG_LEVEL=DEBUG
# ENABLE_FILE_LOGGING=true

# ========== 生产环境示例配置 ==========
# 生产环境需要更稳定和高效的配置

# 生产环境模型配置
# MAIN_MODEL=doubao-seed-1-6-250615
# TOOL_MODEL=doubao-pro
# FLASH_MODEL=doubao-pro

# 生产环境性能配置（更大的值支持复杂对话）
# MAX_HISTORY=200
# MAX_TOKENS=12000
# TOOL_TIMEOUT=60.0

# 生产环境重试配置（更多重试次数提高稳定性）
# MAX_RETRIES=5
# RETRY_BACKOFF=1.5

# 生产环境日志配置
# LOG_LEVEL=INFO
# ENABLE_FILE_LOGGING=true
# LOG_ROTATION_SIZE=50MB

# ========== 特殊场景配置示例 ==========
# 根据不同使用场景，可以调整以下配置

# 快速响应场景（优先响应速度）
# MAIN_MODEL=doubao-pro
# TOOL_MODEL=doubao-pro
# FLASH_MODEL=doubao-pro
# MAX_HISTORY=30
# MAX_TOKENS=2000
# TOOL_TIMEOUT=10.0

# 深度分析场景（优先分析质量）
# MAIN_MODEL=doubao-seed-1-6-250615
# TOOL_MODEL=doubao-seed-1-6-250615
# FLASH_MODEL=doubao-pro
# MAX_HISTORY=500
# MAX_TOKENS=32000
# TOOL_TIMEOUT=120.0

# 批量处理场景（优先稳定性）
# MAX_RETRIES=10
# RETRY_BACKOFF=1.2
# TOOL_TIMEOUT=300.0
# LOG_LEVEL=WARNING
